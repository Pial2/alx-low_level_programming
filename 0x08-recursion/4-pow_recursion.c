#include "main.h"

/**
 * _pow_recursion -> power using recursion
 * @x: x ^ y
 * @y: the power number
 * Return: power results
 */
int _pow_recursion(int x, int y)
{
	if (y < 0)
		return (-1);

	else if (y == 0)
		return (1);
}
